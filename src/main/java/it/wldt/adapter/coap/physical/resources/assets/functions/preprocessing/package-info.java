/**
 * Package containing the body preprocessing functions that can be passed to the physical adapter by the user.
 * These functions will be applied during the transaction between the CoAP protocol and the digital twin,
 * making it possible to have the data in the same format independently of the protocol content format.
 */
package it.wldt.adapter.coap.physical.resources.assets.functions.preprocessing;